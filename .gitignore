node_modules/
dist/
coverage/
.tmp-coverage/
*.tgz
*.log
.DS_Store

# Hot Sheet's local working state: sqlite db, telemetry, multi-MB snapshots,
# rolling backups, and secret.json (a live API secret). None of it is source.
.hotsheet/
